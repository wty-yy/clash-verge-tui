# 更新记录

[English](CHANGELOG.md)

## 未发布

后续计划见 [功能对照](docs/FEATURES.md)。

## v1.1.0 · 2026-09-07

### 自管内核与 Linux 发行版

- 默认启动真实自管工作区；`--demo` 保留为显式界面演示，公开 CLI 不再提供外部控制器连接入口。
- 固定 Clash Verge Rev v2.5.2 对应的 Mihomo v1.19.29，支持 x86_64/aarch64；自动发现发行包内核，缺失时从官方 Release 下载。
- 校验上游压缩包和解压后二进制 SHA-256；工作区内核损坏、被替换或独立升级后自动恢复为应用固定版本。
- 首次启动创建 `0600` 权限的权威工作区清单，并继续私有保存订阅、增强、运行配置、密钥和界面偏好。
- systemd 服务不再保存用户提供的内核路径，后台进程与前台 TUI 使用同一自管内核和工作区。
- 添加 Linux 组合包、校验文件、x86_64/aarch64 自动发布工作流，以及 `curl | sh` 一键安装脚本。
- 设置页内核更新改为显示随应用同步策略；GeoData 与 WebUI 更新仍可独立执行。

## v1.0.1 · 2026-09-07

### 修复

- 远端备份历史改用键值倒序排序，兼容 CI 使用的新版本 Clippy。

## v1.0.0 · 2026-09-07

### 完成 Linux 功能版

- 接入 GNOME 系统代理、PAC、守卫与恢复，TUN、DNS、LAN、端口和流量隧道。
- 添加 systemd 用户服务、登录自启、前台附加与内核崩溃恢复。
- 添加本地加密备份、10 份保留、WebDAV 上传/列表/恢复/删除和损坏文件处理。
- 添加网页可达性/地区检测、GeoData、独立内核升级、WebUI、更新检查、日志轮转与诊断导出。
- 内部使用 Unix socket；控制器地址和密钥重启后生效；升级不覆盖系统安装的内核。
- 完善大型配置编辑、轻量采样、自动测速和失败回滚；验收范围与限制见完成清单。

## v0.3.0 · 2026-09-07

### 新增

- 真实订阅新建、编辑、删除、排序、本地文件/YAML 导入、用量详情和定时刷新。
- YAML / JavaScript 配置增强，按顺序执行，内核校验后应用；失败保留原配置。
- 空工作区直连启动、原子工作区清单、进程间锁、独立校验目录及 Linux 父进程退出清理。
- 连续操作时保留未保存表单，避免上一项未完成时丢失输入。

## v0.2.0 · 2026-09-07

### 新增

- `--connect` 附加现有 HTTP(S) 控制器，支持密钥文件或 `MIHOMO_SECRET` 认证；`--check` 提供无界面诊断。
- `--core` 使用本机 mihomo 和独立目录运行，默认回环端口 17897 / 19097，退出时停止子进程。
- JSON 订阅清单导入、Clash YAML 校验、原文缓存、部分失败保留及显式订阅下载代理。
- 真实策略组成员、手动节点选择、组测速、模式切换、连接详情与关闭、规则启停和集合更新。
- 实际流量速率、累计用量、内存、日志流与自动重连；后台网络请求独立于界面输入。
- 独立模式订阅页重读本地列表并切换已缓存配置；UI 偏好与演示数据分离保存。

### 改进

- 未接入的真实操作明确提示，不再回退到模拟成功；界面按事件和数据变化重绘，改善大规则列表体验。
- 控制器 JSON 隐藏认证字段；订阅与运行配置使用私有文件，测试凭据不进入 Git。
- 新增认证、错误处理、URL 编码、代理下载、重连、组成员和真实操作回归；两份用户提供的订阅在独立实例中实测。

### 范围

- 当前支持 Clash YAML；Base64 / URI 订阅、自动更新、配置增强、系统代理、TUN 管理、WebDAV 和解锁检测仍待接入。
- `--core` 为受界面管理的子进程；需要常驻内核时使用 `--connect` 连接独立服务。

## v0.1.5 · 2026-09-07

### 仓库整理

- 整理 GitHub 仓库：保留远端 MIT LICENSE，将原 GPL 文本归档到 `docs/LICENSE-GPL-3.0`。
- 提交摘要统一为 `v版本号: English summary`；主分支与远端保持为 `master`。
- 新增根目录 `AGENTS.md`，记录交互要求、项目设计与协作规范。

### 改进

- 2–8 页列表与表头取消隔行留白，缩减标签栏、工具栏和详情区域的多余间距。
- 备份历史、页面跳转列表和设置表单同步采用紧凑间距，保留左侧导航的大点击区域。
- 同步调整鼠标命中范围与滚动偏移，保持单击选择和双击确认位置准确。

## v0.1.4 · 2026-09-06

### 改进

- 左侧导航项扩大为 3 行高的整块点击区域，选中项增加边框、背景高亮和加粗文字。
- 短窗口使用 2 行布局，保证 8 个入口均可见；底部状态根据剩余空间显示，避免与导航重叠。

## v0.1.3 · 2026-09-06

### 修复

- 首页“进入订阅管理”首次点击仅高亮，获得焦点后再次点击才进入，不受双击时间限制。
- 切回左侧后需重新选中入口；键盘 Enter 行为保持不变。

## v0.1.2 · 2026-09-06

### 新增

- 首页支持左右区域焦点切换、高亮和订阅卡片 Enter 跳转；小窗口也显示订阅管理入口。
- 代理组、日志等级、设置分类等支持左右切换提示；Vim 模式新增 `h/l` 左右导航，表单文本输入不受影响。
- 列表、首页快捷控制、设置项和备份历史支持鼠标双击，行为等同 `Enter`；单击仅选择。
- 双击需在 400 毫秒内命中同一条目附近；键盘操作、拖动、滚动、缩放或切换页面后重新计次。
- 按钮保留单击行为，备份恢复保留确认弹窗。

## v0.1.1 · 2026-09-06

### 修复

- 流量图按可用宽度重采样，扩大终端后铺满图表；图表高度与坐标标签随布局调整。
- 设置开关、代理选择、当前订阅和规则状态统一使用勾选框。
- Vim 导航启用时在底栏显示 `j/k` 上下选择提示，关闭时隐藏。
- 小窗口提示中的版本号改为读取包版本。

## v0.1.0 · 2026-09-06

### 新增

- 八个终端页面：首页、代理、订阅、连接、规则、日志、解锁检测、设置。
- 深色与浅色主题、强调色、紧凑导航、窗口缩放适配。
- 键盘与鼠标导航、搜索过滤、页面跳转、快捷键帮助。
- 订阅与规则增删改、增强链编辑与排序、连接详情与关闭确认。
- DNS、TUN、端口、外部控制器、内核、主题、备份等设置表单。
- 中文输入、多行编辑、基础表单校验、独立状态保存与本地演示备份历史、恢复、删除。
- 文本与 SVG 页面快照、界面预览图、GitHub Actions 检查流程。
- 页面、表单、交互和持久化回归测试；双语 README 与 CHANGELOG。

### 范围

- 所有网络数据与操作均为本地演示，尚未接入 mihomo。
- 订阅下载、配置增强执行、TUN 权限、系统代理、WebDAV、内核更新待实现。
- 桌面托盘、窗口样式等功能使用终端映射或标记为不适用，详见功能对照。
