# Clash Verge TUI 项目约定

适用于本仓库。用户在当前会话中的明确要求优先于本文；后续修改应延续已经确认的交互和文档风格。

## 目标与当前阶段

- 目标：使用 mihomo 内核，逐步提供与 Clash Verge Rev 功能对应的终端客户端。
- 参考基线：Clash Verge Rev `v2.5.2` / `28f2efc`。参考仓库位于 `upstream/clash-verge-rev`，由 Git 忽略，不纳入本项目提交。
- 当前为 `v0.1.x` 界面预览阶段：先完善八个页面及二级交互，再接入真实内核与系统服务。具体范围见 `docs/FEATURES.md`。
- 演示数据必须明确标识；不能将模拟测速、订阅刷新、解锁检测或系统代理状态描述为真实网络结果。
- 演示状态与实际 Clash Verge 配置分离。界面开发和测试使用独立目录，不启动或修改用户正在使用的代理配置来验证演示交互。

## 设计思路与代码边界

- Rust + Ratatui + Crossterm 实现 TUI。未来 mihomo 作为独立后台进程，终端界面和普通命令行操作复用业务逻辑。
- `src/model.rs`：类型与固定演示数据。
- `src/app.rs`：页面状态、动作、输入处理、表单校验和本地演示流程。
- `src/ui.rs`：布局、主题、表格、图表、弹窗、可点击区域；避免在渲染层增加网络副作用。
- `src/settings.rs`：集中定义设置分类和表单字段。
- `src/storage.rs`：演示状态加载和原子保存；保留读取失败的原文件。
- `src/main.rs`：命令行入口、终端生命周期、事件循环和快照导出。
- 新能力优先扩展清晰的状态与动作边界；真实 mihomo API、配置处理和系统服务应与渲染逻辑分离。
- 主页面：1 首页、2 代理、3 订阅、4 连接、5 规则、6 日志、7 解锁检测、8 设置。

## 已确认的界面与交互要求

### 布局与密度

- 左侧导航使用大点击区域：终端高度至少 32 行时每项 3 行，更矮时每项 2 行。
- 选中导航项整块高亮、文字加粗，并使用边框或左侧指示线；边框和上下留白都属于点击区域。
- 8 个导航入口在最低支持尺寸 `76 × 24` 下仍须可见，底部状态不能覆盖导航。推荐尺寸为 `120 × 40`。
- 主内容与导航采用不同密度：2–8 页列表使用连续单行，不在条目之间插入空白行。
- 备份历史、页面跳转列表同样紧凑排列；设置表单去除字段间多余空行，同时保留字段标签和多行编辑区域。
- 调整行高或间距时，同步修改鼠标命中区域、表头偏移、滚动容量和选中项定位。
- 支持深浅主题、中文双宽字符和终端缩放；避免硬编码空格来定位右侧内容。

### 流量图与选中标记

- 流量趋势随可用宽高缩放，不能固定绘制 60 列后在右侧留下空白。
- 将同一段历史采样重采样至图表宽度，保留首尾时间范围；缩放本身不能推进历史时间或生成新的数据。
- 时间标签对齐图表两端。
- 设置开关、节点选择、当前订阅和规则状态使用 `[✓]` / `[ ]`，不用圆圈表示选中；普通状态指示与选中状态区分。

### 键盘

- 默认启用 Vim 导航：`j/k` 上下选择，`h/l` 左右切换。底栏随启用状态显示或隐藏相应提示。
- 保留方向键与 `Tab` / `Shift+Tab`；`1`–`8` 切换主页面。
- 首页 `←/→` 在快捷控制和右侧订阅卡片间切换，保留左侧选中行；显示当前区域焦点。
- 右侧订阅卡片有焦点时，`Enter` 进入订阅管理。
- 日志的全部 / INFO / DEBUG 等标签、代理组、订阅分类、规则分类、解锁分类和设置分类支持左右切换。
- `h/j/k/l` 在文本表单中仍是正常输入，不得被页面快捷键截获。

### 鼠标

- 普通列表单击仅选中；双击执行与 `Enter` 完全相同的动作，包含已有确认弹窗。
- 双击条件：400 毫秒内点击同一条目、同一行且水平偏移不超过 2 列；不能将快速点击不同条目识别为双击。
- 键盘输入、拖动、滚动、缩放、页面切换等操作重置双击计次；三连击不能重复执行已经确认的操作。
- 首页“进入订阅管理”按钮是特别约定：第一次点击只高亮，已有焦点时再次点击才进入，两次点击没有时间限制。
- 焦点切回首页左侧后，订阅管理按钮需要重新选中。若已通过键盘获得右侧焦点，点击按钮可以直接进入。
- 其他工具按钮沿用各自的单击行为；鼠标操作不能绕过备份恢复等确认流程。

## Git 与版本维护

- 仓库：`https://github.com/wty-yy/clash-verge-tui.git`；主分支及远端默认分支：`master`。
- 每次提交说明必须以版本号开头，摘要使用英文：`v0.1.5: Compact content lists and settings forms`。
- 不使用 `feat:`、`fix:`、`docs:` 等作为提交开头；不要使用中文提交摘要。同一版本内允许多个提交共用版本号。
- 版本从 `v0.1.0` 起迭代，使用语义化版本和带注释的 Git 标签。
- 版本更新同步 `Cargo.toml`、`Cargo.lock`、双语 CHANGELOG 和必要的 README 内容；涉及界面时更新预览图。
- 已发布标签不移动，不为整理提交信息而擅自重写已推送历史。用户明确要求重写时，先保留可恢复的 Git 历史备份。
- 合并远端已有内容时保留其提交历史；不要用强制推送覆盖远端初始化内容。
- 详细流程见 `docs/RELEASING.md`。

## 文档与许可

- README 遵循 `wty-readme-generator` 技能：简短直接、介绍 → 使用 → 实现细节 → 开发与版本；命令解释尽量写为代码块内注释。
- 同步维护 `README.md`（英文）和 `README.zh-CN.md`（中文），顶部互相链接，章节顺序、命令、路径、默认行为与限制一致。
- 同步维护 `CHANGELOG.md` 与 `CHANGELOG.zh-CN.md`，使用简短的版本摘要；历史记录不冒充当前功能状态。
- 根目录 `LICENSE` 保留用户在 GitHub 创建的 MIT 许可文本与版权信息。
- 此前的 GPL 许可文本原样保存于 `docs/LICENSE-GPL-3.0`，不删除、不覆盖；当前项目许可字段和关于页面与根目录 LICENSE 一致。
- 后续新增实际依赖或引入上游实现时，核对其来源与许可，保留必要的归属信息。

## 验证与交付

- 交互或布局修改补充针对性回归：实际可见行与点击位置、滚动、缩放、确认流程和键盘 / 鼠标行为一致性。
- 文档或 Git 元数据修改以一致性检查为主，不因其重复运行无关测试。
- 代码发布前执行：

```bash
cargo fmt --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked
cargo build --locked --release
```

- 快照来自实际 Ratatui 缓冲区；使用 `--snapshot` 导出页面，验证过程中不得读取或保存用户状态。
- 界面更新检查标准窗口和最小窗口；事件处理更新必要时在真实 PTY 中验证输入和退出后的终端恢复。
- 验证记录保存在 `docs/VALIDATION.md`，区分本地测试和真实远端 CI 结果。
- 交付说明包含完成内容、当前版本、必要限制与验证结果；按用户要求推送分支和版本标签，不自动创建 GitHub Release。
