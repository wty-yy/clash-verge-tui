# 完成验收清单

验收基线：Linux 上与 Clash Verge Rev v2.5.2 对应的终端功能。桌面窗口、托盘图形、CSS、UWP 等不适用项须明确说明，不以演示实现充当真实能力。

| 模块 | 验收内容 | 状态 |
| --- | --- | --- |
| 界面 | 八页、紧凑列表、导航点击区、双击、键盘焦点、中文、缩放 | 已验证 |
| 内核连接 | 认证、真实指标与日志、节点、规则、连接、重连 | 已验证 |
| 订阅管理 | 远程和本地导入、编辑、删除、排序、用量、按需和定时更新 | 已验证 |
| 配置增强 | YAML 覆写、JavaScript、排序、启停、语义校验、失败回滚 | 已验证 |
| 网络设置 | DNS、端口、局域网、控制器、流量隧道、持久化 | 已验证 |
| 系统代理 | GNOME 设置、绕过、PAC、守卫、恢复 | 已验证 |
| TUN | 权限提示、真实启停、路由参数、隔离网络命名空间测试 | 已验证 |
| 服务与启动 | 独立后台服务、自启动、启停、恢复及清理 | 已验证 |
| 备份 | 本地创建、列表、恢复、删除、WebDAV、失败处理 | 已验证 |
| 解锁检测 | 各服务实际请求、地区/可用性区分、不确定结果处理 | 已验证 |
| 更新与诊断 | GeoData、内核通道、日志导出、诊断、目录/WebUI 打开 | 已验证 |
| 发布 | 回归、实际订阅测试、文档、凭据检查、提交与推送 | 本地验收通过；提交、标签和 CI 以 GitHub 记录为准 |

对应证据见 `docs/VALIDATION.md`。服务测试单元已卸载，未修改原 Clash Verge 配置。

逐项保留验证证据。权限、环境或远端服务导致无法验证的项目标记为待验证，不宣称整体完成。
