# v0.1.0 验证记录

环境：Linux，Rust 1.93.1，终端模拟模式。日期：2026-09-06。

## 自动检查

- `cargo fmt --check`：通过。
- `cargo clippy --locked --all-targets -- -D warnings`：通过。
- `cargo test --locked`：17 项集成测试通过。
- `cargo build --locked --release`：通过。
- `clash-verge-tui --version`：`0.1.0`。

测试范围：八个页面及所有分组、深浅主题、76×24 / 100×30 / 120×40 / 180×52 四种尺寸；全部设置表单滚动与默认值保存；中文编辑；筛选后条目定位；订阅与规则增删改；增强链排序；连接关闭确认；日志暂停；分类检测；备份历史与恢复；独立持久化；损坏文件保留；命令行参数与确定性快照。

## 真实终端检查

在独立临时状态目录运行发布二进制，通过系统 PTY 输入并检查保存结果：

- 八个主页面切换。
- Bracketed paste 输入订阅名称和 URL，提交保存。
- 取消关闭全部连接后保留原会话。
- 解锁检测演示流程和本地备份。
- 弹窗打开期间 120×40 → 76×24 → 120×40 缩放。
- 正常退出、离开备用屏幕、恢复原始 termios 设置。

## 视觉与文档检查

- 八个页面使用实际 Ratatui 缓冲区生成 SVG；PNG 由对应内容渲染用于预览。
- 已检查中文双宽字符、表格选中状态、主页图表及八页预览总览。
- README 中英文命令一致，文档相对链接有效。
- GitHub Actions 已配置；本记录为本地检查结果，尚未运行远端 CI。

未验证：真实 mihomo 连接、系统代理、TUN、订阅下载、WebDAV 网络同步与其他操作系统。

## v0.1.1 补丁验证 · 2026-09-06

- 20 项集成测试通过；Clippy、发布构建与格式检查通过。
- 新增同一终端反复缩放回归：120×40 → 180×52 → 76×24 → 220×70 → 120×40，检查图表每一列均有绘制、坐标右对齐、历史不因缩放推进。
- 新增勾选框渲染、当前订阅完整显示与开关切换回归。
- 新增 Vim 开关与 `j/k` 提示、按键行为一致性回归。
- 更新八页预览，另检查 180×52 首页放大效果。

## v0.1.2 交互验证 · 2026-09-06

- 31 项自动化测试通过（5 项单元测试、26 项集成测试）；Clippy、格式检查和发布构建通过。
- 双击与 Enter 在全部主页面、页内分组和备份恢复上的行为一致；单击不执行行操作。
- 检查双击时限、点击位置、释放事件、第三次点击、筛选排序、鼠标关闭，以及输入、滚动、拖动、缩放后的计次重置。
- 首页左右焦点与 Tab 切换保留左侧选中项，小窗口保留订阅管理按钮；Vim h/l 不影响表单文本输入。
- 全部页内标签支持左右切换和边界循环，日志等级过滤结果符合当前标签。
- 发布二进制在真实 PTY 中通过 SGR 鼠标双击打开系统代理表单并保存，首页 Right + Enter 跳转后创建订阅，左右切换日志等级，退出后恢复终端设置。
- 更新八页预览并检查首页焦点样式。

## v0.1.3 入口点击验证 · 2026-09-06

- 32 项自动化测试、Clippy、格式检查与发布构建通过。
- 新增首页实际按钮命中区域的鼠标回归，覆盖 76×24 和 120×40：首次点击只高亮，切回左侧后需重新选中，清除双击计时后再次点击仍可进入，键盘已聚焦时点击可直接进入。

## v0.1.4 导航区域验证 · 2026-09-06

- 33 项自动化测试、Clippy、格式检查与发布构建通过。
- 在 24、30、32、34、40、52 行窗口检查 8 个导航入口，覆盖每项左右边缘及上下留白的点击，确认跳转正确、点击区域不重叠。
- 检查选中项整块背景与边框，并更新八页预览。

## v0.1.5 紧凑布局验证 · 2026-09-07

- 35 项自动化测试、Clippy、格式检查与发布构建通过。
- 验证 2–8 页全部分组在小窗口和标准窗口下连续单行显示，滚动后仍保留选中项。
- 使用 40 条独立日志核对可见文字、鼠标命中位置与滚动偏移，避免行距调整造成错选。
- 验证备份历史和页面跳转列表连续排列、表单字段之间无多余空行，同时保留左侧导航的大点击区域。
- 更新八页预览。

## v0.2.0 mihomo 连接验证 · 2026-09-07

环境：Linux，Rust 1.93.1，本机 `verge-mihomo` v1.19.29。46 项自动化测试、Clippy、格式检查和发布构建通过。所有实际网络测试在独立目录和 127.0.0.1:17897 / 19097 上执行。

- 自动化回归覆盖原有交互，以及认证头、特殊名称 URL 编码、401 / 403 错误脱敏、订阅下载代理、部分导入失败保留、独立配置约束、日志流、断连恢复、后台线程停止、真实组成员、规则索引、连接 UUID 和 UI 偏好分离。
- 订阅 1：下载 35 个节点、3 个订阅策略组、914 条规则；内核 API 返回 45 个代理条目和 4 个策略组（含内置项）。
- 订阅 2：直接请求和原有访问线路返回 Cloudflare 403；通过订阅 1 的代理线路，使用本程序 `--subscription-proxy` 成功下载 72 个节点、9 个订阅策略组、9,263 条规则。
- 从真实 TUI 重读订阅列表并切换到订阅 2，内核 API 返回 88 个代理条目、9,263 条规则；当前订阅索引正确保存。
- 订阅 1 的抽样测速得到 324 ms、761 ms；两个节点通过显式本地代理的 curl HTTPS 请求均返回 204。
- 订阅 2 的实际节点抽样：3 个 VLESS 节点分别为 175 ms、155 ms、257 ms，1 个 Hysteria2 节点超时；成功节点的 curl HTTPS 请求返回 204。未对全部节点可用性作保证。
- 在真实 PTY 中验证 LIVE 页面、节点选择、模式切换、规则启停、活动连接出现与关闭、大规则列表响应、窗口缩放、退出及终端恢复。
- 订阅 URL、节点信息、密钥、配置和原始日志只保存在 Git 忽略的本地私有目录；本记录不包含这些凭据。
- 尚未验证系统代理、TUN 管理和其他未接入功能；原有 Clash Verge 实例未被接管。

## v1.0.0 Linux 功能验收 · 2026-09-07

58 项自动化测试、格式检查、Clippy 与发布构建通过。

- 真实工作区：空配置启动、界面订阅导入、YAML/JavaScript 生效、禁用脚本、非法配置回滚、定时更新、重启恢复。
- 配置校验使用独立目录与资源副本；网页目录在校验沙箱内映射，避免与运行缓存和路径限制冲突。
- GNOME 系统代理在私有 keyfile 数据库测试设置、守卫和恢复；GIO 原生代理解析器验证 PAC 为普通网站返回代理、为绕过域名返回直连。
- 独立网络命名空间实测 TUN 启用/禁用及清理、DNS 应答、LAN 地址监听、端口切换、TCP 隧道，以及关闭外部 HTTP 后通过私有 socket 重启连接。
- systemd 用户服务实测安装、启动、附加诊断、退出界面保持运行、杀死测试内核后自动恢复、停止与卸载。测试单元已卸载。
- 加密备份与 WebDAV 控制服务器测试覆盖上传、远端列表、错误密码不修改状态、恢复、删除和路径校验。
- GeoData 更新返回 204，WebUI 下载返回 200 并生成 160 个文件；工作区内核从 v1.19.29 更新为 v1.19.30，系统 `/usr/bin/verge-mihomo` 仍为 v1.19.29。
- 通过已测速的真实节点测试 8 个网页服务：Netflix、YouTube Premium、Disney+、Spotify、Gemini 页面可达；ChatGPT、Claude、TikTok 返回验证页面，正确标记为未知而非已解锁；出口地区为 US。
- 已验证两个私有订阅的加载与抽样出站；测试 URL、节点凭据、原始配置、密钥与终端原始输出仅在 `.local/`，不纳入发布。
- 系统集成验收限定 Linux/GNOME；TUN 权限以隔离命名空间验证，不对主机真实路由做切换。网页检测不代表账号或付费播放授权。

## v1.1.0 自管内核与发行验收 · 2026-09-07

- 61 项自动化测试通过；格式、Clippy、发布构建和 CLI 帮助检查通过。公开帮助中不再出现 `--connect` 或用户内核参数。
- 使用官方 `mihomo-linux-amd64-v1.19.29.gz` 验证压缩包 SHA-256、解压后二进制 SHA-256 和 `mihomo -v`；两个哈希均写入应用，arm64 对应哈希写入同一发行定义。
- 在临时 HOME 构建 Linux x86_64 组合包，通过同一 `scripts/install.sh` 从本地发行资产安装；已安装程序自动发现 `~/.local/lib/clash-verge-tui/core/v1.19.29/mihomo` 并完成 `--check`。
- 使用没有预置内核的源码构建验证首次启动官方下载；缓存和工作区副本权限均为 `0700`，权威清单、版本关系和控制密钥均为 `0600`。
- 用 `/bin/true` 覆盖工作区内核后再次启动，程序按固定二进制哈希恢复官方 v1.19.29；`--check` 退出后代理和控制端口均已释放。
- systemd 单元只保存应用和数据目录，不保存外部内核路径；无参数前台启动和 daemon 使用同一工作区解析规则。
- GitHub Release 工作流包含原生 Linux x86_64 与 aarch64 runner，远端双架构构建、资产发布及公开一键安装在推送 v1.1.0 标签后记录。

## v1.2.0 订阅链接导入验证 · 2026-09-07

- 66 项自动化测试覆盖既有功能和本次新增流程；格式、Clippy、测试及 release 构建均按发布门槛执行。
- 自动化回归覆盖链接字段与按钮渲染、点击动作、Enter 导入、无效 URL、异步工作线程、表单保留和 YAML 内容回填。
- 模拟服务器验证下载返回完整原文，并在写入表单前完成 Clash YAML 校验；下载动作不创建订阅文件。
- 用户提供的 HTTPS 链接返回 200，通过真实导入代码得到 35 个节点、3 个策略组；私有配置以 `0600` 写入临时目录，仅用于核对下载层。
- 在 `120 × 40` 真实 PTY 中使用鼠标点击“导入”和“Ctrl+S 保存”，完整经历下载、独立内核校验、GeoSite/MMDB 首次准备、运行配置应用和事务提交；退出后测试端口已释放。
- 重新构建 v1.2.0 x86_64 应用/内核组合包，在临时 HOME 通过发行版 `install.sh` 安装，并由已安装程序完成 Mihomo v1.19.29 `--check`。
- 测试链接、响应头、原始 YAML 和节点信息均未写入源码、测试夹具、文档或 Git 历史。

## v1.3.0 首页端口与 TUN 权限服务验证 · 2026-09-07

- 72 项自动化测试覆盖首页端口行、最小窗口滚动、`s` 保存应用、无效端口、TUN 服务确认、TUI 密码表单、密码 Debug 去敏、服务单元和 CLI 状态。
- 在隔离网络命名空间以新工作区无参数启动，`--check` 返回 `mixed_port: 7890` 与 Mihomo v1.19.29；未占用或修改主机现有 7890 监听器。
- 当前主机 `/dev/net/tun`、sudo、systemd、getcap/setcap 均存在；失败工作区的官方内核没有 `CAP_NET_ADMIN`，确认旧提示来自缺少能力和 1 秒网卡等待时间。
- 将网卡创建/移除等待提高到 5 秒并补充明确状态；在用户/网络隔离命名空间完整通过 TUN 创建/关闭、DNS、LAN 绑定、端口切换、TCP 隧道和私有控制器重启。
- 生成并检查按 UID/工作区散列命名的 systemd `.path`/`.service`：root 助手限制为 `CAP_SETFCAP`，验证官方内核哈希、文件类型、所有者和写权限，只授予网络管理/绑定能力。
- 在隔离用户命名空间以临时官方内核执行隐藏 root 助手，成功得到 `cap_net_bind_service,cap_net_admin=ep`；未修改用户正在运行的工作区内核。
- 图形 PolicyKit 路径已移除；TUI 密码字段使用 Secret 遮罩，命令 Debug 去敏，密码从字段取出后只通过管道发送给 `sudo -S`，相关字节和命令对象在使用后清零。
- 未在自动测试中使用或记录真实系统密码；最终 sudo 授权由用户在已发布 TUI 的密码框内完成。
- 重新构建 v1.3.0 x86_64 组合包并在临时 HOME 安装；已安装程序通过 Mihomo v1.19.29 `--check`，同时包含首页端口与终端内 TUN 密码快照。

## v1.3.1 订阅导入入口可见性 · 2026-09-07

- 73 项自动化测试覆盖既有流程，以及真实/演示新增表单的字段顺序、默认焦点、导入按钮命中区和工具栏入口。
- 在 120×40 与最低 76×24 快照中，第一行均完整显示“订阅文件链接”和 `[ 导入 ]`，下方紧接“订阅配置 YAML”。
- 真实远程订阅编辑表单读取已保存的完整 YAML；名称、代理、更新间隔和本地文件字段移到配置区之后。
- 更新 `profile-import.svg` 并使用浏览器渲染检查中文、按钮边界、输入焦点、YAML 编辑区和底部操作提示。
- GitHub TLS 握手出现一次瞬时失败后，补充全错误重试并成功生成、安装 v1.3.1 x86_64 组合包；安装后二进制的 76×24 快照保持相同布局。

## v1.3.2 订阅导入点击与自动命名 · 2026-09-07

- 75 项自动化测试通过；新增测试在 120×40 和最低 76×24 窗口向 `[ 导入 ]` 命中区发送真实鼠标按下事件，不再只检查动作是否存在。
- 验证订阅下载可在前一个 mihomo 操作完成前排队；其完成事件不会清除下载请求，随后仍会将 YAML 与名称写回原表单。
- 验证空名称按订阅标题、附件文件名、配置名称、来源域名和 IP 通用名称的顺序补全，同时保留用户已输入名称。
- `cargo fmt --check`、Clippy 严格警告、完整测试和 release 构建通过；重新生成 v1.3.2 订阅导入 SVG 快照。
- 生成 v1.3.2 x86_64 应用/内核组合包，在隔离目录通过 `install.sh` 安装；已安装程序以独立端口完成 Mihomo v1.19.29 `--check`，退出后端口释放且无残留进程。

## v1.3.3 TUN 权限服务安装修复 · 2026-09-07

- 检查本机失败残留确认 sudo 已完成：root helper 与 systemd 单元已经写入；`.path` 为 `bad-setting`，日志显示带引号的 `PathChanged` 被视为非绝对路径。
- 服务日志同时显示 helper 读取私有 `0700` 内核时返回 `Permission denied`；能力边界补充 `CAP_DAC_READ_SEARCH` 与 `CAP_FOWNER`，仍只用 `CAP_SETFCAP` 修改文件能力。
- 新增 systemd 路径转义、生成单元验证、sudo 密码/策略/helper 错误分类回归；生成的含空格工作区单元通过本机 `systemd-analyze verify`。
- 77 项自动化测试以及格式、Clippy 严格警告和 release 构建通过；重新生成 v1.3.3 TUN 密码弹窗 SVG 快照。
- 生成 v1.3.3 x86_64 应用/内核组合包，在隔离目录通过 `install.sh` 安装，并以独立端口完成 Mihomo v1.19.29 `--check`。
- 验证未读取、使用或记录真实系统密码；发布安装后由用户在 TUI 密码框重新执行一次修复后的授权流程。
