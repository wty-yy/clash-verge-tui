# v0.1.0 验证记录

环境：Linux，Rust 1.93.1，终端模拟模式。日期：2026-09-06。

## 自动检查

- `cargo fmt --check`：通过。
- `cargo clippy --locked --all-targets -- -D warnings`：通过。
- `cargo test --locked`：17 项集成测试通过。
- `cargo build --locked --release`：通过。
- `clash-verge-tui --version`：`0.1.0`。

测试范围：八个页面及所有分组、深浅主题、76×24 / 100×30 / 120×40 / 180×52 四种尺寸；全部设置表单滚动与默认值保存；中文编辑；筛选后条目定位；订阅与规则增删改；增强链排序；连接关闭确认；日志暂停；分类检测；备份历史与恢复；独立持久化；损坏文件保留；命令行参数与确定性快照。

## 真实终端检查

在独立临时状态目录运行发布二进制，通过系统 PTY 输入并检查保存结果：

- 八个主页面切换。
- Bracketed paste 输入订阅名称和 URL，提交保存。
- 取消关闭全部连接后保留原会话。
- 解锁检测演示流程和本地备份。
- 弹窗打开期间 120×40 → 76×24 → 120×40 缩放。
- 正常退出、离开备用屏幕、恢复原始 termios 设置。

## 视觉与文档检查

- 八个页面使用实际 Ratatui 缓冲区生成 SVG；PNG 由对应内容渲染用于预览。
- 已检查中文双宽字符、表格选中状态、主页图表及八页预览总览。
- README 中英文命令一致，文档相对链接有效。
- GitHub Actions 已配置；本记录为本地检查结果，尚未运行远端 CI。

未验证：真实 mihomo 连接、系统代理、TUN、订阅下载、WebDAV 网络同步与其他操作系统。

## v0.1.1 补丁验证 · 2026-09-06

- 20 项集成测试通过；Clippy、发布构建与格式检查通过。
- 新增同一终端反复缩放回归：120×40 → 180×52 → 76×24 → 220×70 → 120×40，检查图表每一列均有绘制、坐标右对齐、历史不因缩放推进。
- 新增勾选框渲染、当前订阅完整显示与开关切换回归。
- 新增 Vim 开关与 `j/k` 提示、按键行为一致性回归。
- 更新八页预览，另检查 180×52 首页放大效果。

## v0.1.2 交互验证 · 2026-09-06

- 31 项自动化测试通过（5 项单元测试、26 项集成测试）；Clippy、格式检查和发布构建通过。
- 双击与 Enter 在全部主页面、页内分组和备份恢复上的行为一致；单击不执行行操作。
- 检查双击时限、点击位置、释放事件、第三次点击、筛选排序、鼠标关闭，以及输入、滚动、拖动、缩放后的计次重置。
- 首页左右焦点与 Tab 切换保留左侧选中项，小窗口保留订阅管理按钮；Vim h/l 不影响表单文本输入。
- 全部页内标签支持左右切换和边界循环，日志等级过滤结果符合当前标签。
- 发布二进制在真实 PTY 中通过 SGR 鼠标双击打开系统代理表单并保存，首页 Right + Enter 跳转后创建订阅，左右切换日志等级，退出后恢复终端设置。
- 更新八页预览并检查首页焦点样式。

## v0.1.3 入口点击验证 · 2026-09-06

- 32 项自动化测试、Clippy、格式检查与发布构建通过。
- 新增首页实际按钮命中区域的鼠标回归，覆盖 76×24 和 120×40：首次点击只高亮，切回左侧后需重新选中，清除双击计时后再次点击仍可进入，键盘已聚焦时点击可直接进入。

## v0.1.4 导航区域验证 · 2026-09-06

- 33 项自动化测试、Clippy、格式检查与发布构建通过。
- 在 24、30、32、34、40、52 行窗口检查 8 个导航入口，覆盖每项左右边缘及上下留白的点击，确认跳转正确、点击区域不重叠。
- 检查选中项整块背景与边框，并更新八页预览。
